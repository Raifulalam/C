//linear search in array//
#include<stdio.h>
int main()
{
    int arr[30];
    int find,count;
    printf("enter the elementes of array");
    for(int i=0;i<=10;i++)
    scanf ("%d",&arr[i]);
    printf("enter the number to find");
    scanf("%d",&find);
    for (int i = 0; i <=9; i++)
    {
        if(a[i]==find)
        {
            printf("element if found %d")
        }
    }  

}
